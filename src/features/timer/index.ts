export {
  createTimer,
  tick,
  start,
  pause,
  resume,
  stop,
  reset,
  addLap,
  getCurrentElapsed,
  getRemainingMs,
  getProgress,
  formatTime,
} from './lib/timerEngine'
export type { TimerState, TimerStatus } from './lib/timerEngine'
