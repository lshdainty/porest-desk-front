/**
 * Prettier — **기본값을 그대로 쓴다.**
 *
 * 서식은 논쟁거리가 아니라 정해 두고 잊는 것이라, gofmt·dart format 처럼 도구의
 * 의견을 그대로 받는다. 이 파일이 비어 있는 건 설정을 빠뜨린 게 아니라 그 결정이다.
 * (파일 자체는 남긴다 — 에디터가 이 레포를 prettier 관리 대상으로 인식하게 하려고)
 *
 * 기본값이 정하는 것 중 눈에 띄는 둘:
 *   - semi: true        → 세미콜론을 붙인다
 *   - singleQuote: false → 큰따옴표를 쓴다
 *   - printWidth: 80
 *
 * 바꾸고 싶으면 아래에 그 키만 적으면 된다. 다만 바꾸는 순간 "왜 이 값인가" 를
 * 설명할 책임이 생긴다 — 기본값은 그 설명이 필요 없다는 게 가장 큰 장점이다.
 */
export default {};
